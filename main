--[[
    XENOHUB V6: CARTOLA EDITION (ULTIMATE BYPASS)
    Yapımcı: Xeno Team
    Özellikler:
    - Cartola Hub Tasarımı (Karanlık & Şık)
    - Full Gamepass Bypass (Dosyalardan Entegre Edildi)
    - Sürüklenebilir Menü ve Buton
    - Sıfır Lag (Optimize Edilmiş Döngüler)
]]

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- ==============================================================================
-- 1. DRAGGABLE (SÜRÜKLEME) FONKSİYONU
-- ==============================================================================
local function MakeDraggable(topbarobject, object)
    local Dragging = nil
    local DragInput = nil
    local DragStart = nil
    local StartPosition = nil

    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + Delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + Delta.Y)
        object.Position = pos
    end

    topbarobject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = object.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    topbarobject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
end

-- ==============================================================================
-- 2. UI TASARIMI (CARTOLA STYLE - KOMPAKT)
-- ==============================================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "XenoCartolaV6"
ScreenGui.ResetOnSpawn = false
pcall(function() ScreenGui.Parent = CoreGui end)

-- AÇMA / KAPAMA BUTONU (Sürüklenebilir)
local OpenBtnFrame = Instance.new("Frame")
OpenBtnFrame.Size = UDim2.new(0, 50, 0, 50)
OpenBtnFrame.Position = UDim2.new(0.1, 0, 0.1, 0)
OpenBtnFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
OpenBtnFrame.BorderSizePixel = 0
OpenBtnFrame.Parent = ScreenGui
Instance.new("UICorner", OpenBtnFrame).CornerRadius = UDim.new(0, 25) -- Yuvarlak

local OpenBtn = Instance.new("ImageButton")
OpenBtn.Size = UDim2.new(0, 30, 0, 30)
OpenBtn.Position = UDim2.new(0.2, 0, 0.2, 0)
OpenBtn.BackgroundTransparency = 1
OpenBtn.Image = "rbxassetid://129326077891103" -- Xeno Logo veya Cartola ikonu
OpenBtn.Parent = OpenBtnFrame

MakeDraggable(OpenBtnFrame, OpenBtnFrame)

-- ANA MENÜ (CARTOLA TARZI)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 600, 0, 350) -- Küçültülmüş Boyut
MainFrame.Position = UDim2.new(0.5, -300, 0.5, -175)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Visible = false -- Başlangıçta gizli
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- Topbar (Sürükleme için)
local Topbar = Instance.new("Frame")
Topbar.Size = UDim2.new(1, 0, 0, 40)
Topbar.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Topbar.BorderSizePixel = 0
Topbar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Text = "Cartola Hub | Xeno Edit"
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = Color3.fromRGB(200, 200, 200)
Title.TextSize = 14
Title.Size = UDim2.new(0.5, 0, 1, 0)
Title.Position = UDim2.new(0.02, 0, 0, 0)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Topbar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Size = UDim2.new(0, 40, 1, 0)
CloseBtn.Position = UDim2.new(1, -40, 0, 0)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 16
CloseBtn.Parent = Topbar

MakeDraggable(Topbar, MainFrame)

-- Yan Menü (Sidebar)
local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Size = UDim2.new(0, 160, 1, -40)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
Sidebar.BorderSizePixel = 0
Sidebar.ScrollBarThickness = 2
Sidebar.Parent = MainFrame

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Padding = UDim.new(0, 5)
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Parent = Sidebar

local SidebarPadding = Instance.new("UIPadding")
SidebarPadding.PaddingTop = UDim.new(0, 10)
SidebarPadding.PaddingLeft = UDim.new(0, 5)
SidebarPadding.Parent = Sidebar

-- İçerik Alanı
local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -170, 1, -50)
Container.Position = UDim2.new(0, 165, 0, 45)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

local Pages = {}

-- Buton Mantığı
OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- Tab Oluşturma
local function CreateTab(name, iconId)
    local TabBtn = Instance.new("TextButton")
    TabBtn.Size = UDim2.new(1, -10, 0, 35)
    TabBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    TabBtn.Text = "  " .. name
    TabBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabBtn.Font = Enum.Font.GothamMedium
    TabBtn.TextSize = 13
    TabBtn.TextXAlignment = Enum.TextXAlignment.Left
    TabBtn.Parent = Sidebar
    Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 6)

    local Page = Instance.new("ScrollingFrame")
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 4
    Page.Parent = Container
    
    local PageLayout = Instance.new("UIListLayout")
    PageLayout.Padding = UDim.new(0, 8)
    PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    PageLayout.Parent = Page
    
    local PagePad = Instance.new("UIPadding")
    PagePad.PaddingTop = UDim.new(0, 5)
    PagePad.PaddingLeft = UDim.new(0, 5)
    PagePad.Parent = Page

    Pages[name] = Page

    TabBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        Page.Visible = true
        
        -- Renk güncelleme
        for _, btn in pairs(Sidebar:GetChildren()) do
            if btn:IsA("TextButton") then
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(25, 25, 25), TextColor3 = Color3.fromRGB(150,150,150)}):Play()
            end
        end
        TweenService:Create(TabBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(145, 90, 230), TextColor3 = Color3.fromRGB(255,255,255)}):Play()
    end)
    
    return Page
end

-- Eleman Oluşturma
local function CreateButton(page, text, callback)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -10, 0, 35)
    Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Btn.Text = text
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.Font = Enum.Font.Gotham
    Btn.TextSize = 13
    Btn.Parent = page
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)
    
    Btn.MouseButton1Click:Connect(function()
        local ripple = Instance.new("Frame")
        ripple.BackgroundColor3 = Color3.fromRGB(255,255,255)
        ripple.BackgroundTransparency = 0.8
        ripple.Size = UDim2.new(0,0,0,0)
        ripple.Position = UDim2.new(0.5,0,0.5,0)
        ripple.Parent = Btn
        Instance.new("UICorner", ripple).CornerRadius = UDim.new(1,0)
        TweenService:Create(ripple, TweenInfo.new(0.3), {Size = UDim2.new(1.2,0,2,0), BackgroundTransparency = 1}):Play()
        task.delay(0.3, function() ripple:Destroy() end)
        callback()
    end)
end

local function CreateToggle(page, text, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, -10, 0, 35)
    Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Frame.Parent = page
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 6)

    local Lbl = Instance.new("TextLabel")
    Lbl.Text = text
    Lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    Lbl.Font = Enum.Font.Gotham
    Lbl.TextSize = 13
    Lbl.TextXAlignment = Enum.TextXAlignment.Left
    Lbl.Size = UDim2.new(0.7, 0, 1, 0)
    Lbl.Position = UDim2.new(0.05, 0, 0, 0)
    Lbl.BackgroundTransparency = 1
    Lbl.Parent = Frame

    local TogBtn = Instance.new("TextButton")
    TogBtn.Text = ""
    TogBtn.Size = UDim2.new(0, 40, 0, 20)
    TogBtn.Position = UDim2.new(1, -50, 0.5, -10)
    TogBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    TogBtn.Parent = Frame
    Instance.new("UICorner", TogBtn).CornerRadius = UDim.new(1, 0)

    local Circle = Instance.new("Frame")
    Circle.Size = UDim2.new(0, 16, 0, 16)
    Circle.Position = UDim2.new(0, 2, 0.5, -8)
    Circle.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    Circle.Parent = TogBtn
    Instance.new("UICorner", Circle).CornerRadius = UDim.new(1, 0)

    local state = false
    TogBtn.MouseButton1Click:Connect(function()
        state = not state
        local color = state and Color3.fromRGB(145, 90, 230) or Color3.fromRGB(50, 50, 50)
        local pos = state and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
        
        TweenService:Create(TogBtn, TweenInfo.new(0.2), {BackgroundColor3 = color}):Play()
        TweenService:Create(Circle, TweenInfo.new(0.2), {Position = pos}):Play()
        task.spawn(function() callback(state) end)
    end)
end

-- Dropdown (Cartola Tarzı)
local function CreateDropdown(page, text, options, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, -10, 0, 35)
    Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Frame.Parent = page
    Frame.ClipsDescendants = true
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 6)

    local MainBtn = Instance.new("TextButton")
    MainBtn.Size = UDim2.new(1, 0, 0, 35)
    MainBtn.BackgroundTransparency = 1
    MainBtn.Text = text .. " ▼"
    MainBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    MainBtn.Font = Enum.Font.Gotham
    MainBtn.TextSize = 13
    MainBtn.Parent = Frame

    local isOpen = false
    MainBtn.MouseButton1Click:Connect(function()
        isOpen = not isOpen
        Frame.Size = isOpen and UDim2.new(1, -10, 0, 35 + (#options * 30)) or UDim2.new(1, -10, 0, 35)
    end)

    for i, opt in ipairs(options) do
        local OptBtn = Instance.new("TextButton")
        OptBtn.Size = UDim2.new(1, 0, 0, 30)
        OptBtn.Position = UDim2.new(0, 0, 0, 35 + ((i-1)*30))
        OptBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        OptBtn.Text = opt
        OptBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
        OptBtn.Font = Enum.Font.Gotham
        OptBtn.TextSize = 12
        OptBtn.Parent = Frame
        
        OptBtn.MouseButton1Click:Connect(function()
            callback(opt)
            MainBtn.Text = text .. ": " .. opt
            isOpen = false
            Frame.Size = UDim2.new(1, -10, 0, 35)
        end)
    end
end

-- ==============================================================================
-- 3. ÖZELLİKLER & BYPASS MANTIĞI
-- ==============================================================================

local FreePassTab = CreateTab("Free Gamepass", "")
local MainTab = CreateTab("Ana Menü", "")
local HouseTab = CreateTab("Ev Kontrol", "")
local VehicleTab = CreateTab("Araçlar", "")
local AvatarTab = CreateTab("Avatar", "")
local TrollTab = CreateTab("Troll", "")

-- FREE GAMEPASS KISMI (RESİMDEKİ GİBİ)
local selectedPass = "Vip" -- Varsayılan

-- Dropdown Listesi (Resimdeki ile aynı)
local Gamepasses = {
    "Vip", "Premium", "Estates", "Vehicle Pack", "Land Unlocked", 
    "Boat Pack", "Vehicle Speed", "Vehicle Customization", 
    "Vehicle Upgrade", "Music Unlocked", "Disaster Pass", 
    "Penthouse", "Theme Pass", "On Fire", "Horses Unlocked", "Faces Unlocked"
}

CreateDropdown(FreePassTab, "Select Gamepass", Gamepasses, function(val)
    selectedPass = val
end)

CreateButton(FreePassTab, "Get Gamepass | AD Button needed", function()
    -- Ads Controller Bypass
    spawn(function()
        local success, Ads = pcall(function() return require(ReplicatedStorage.Modules.Client.Ads.AdvertisementsController) end)
        if success and Ads then
            Ads.GetUnlockedDuration = function() return 72000 end -- 20 Saatlik Bypass
            Ads.IsEligibleForRewardedVideoAd = function() return true end
            Ads.IsRewardedVideoAdReady = function() 
                return require(ReplicatedStorage.Packages.Promise).resolve(Enum.AdAvailabilityResult.IsAvailable) 
            end
        end
    end)
    
    -- Seçili Gamepass'i bul ve aç
    pcall(function()
        local GPModule = require(ReplicatedStorage.Modules.Shared.PlayerData.Gamepasses)
        local GPUnlock = require(ReplicatedStorage.Modules.Client.UI.GamepassUnlockController)
        
        -- Seçilen isme göre ID bul
        for _, gpData in pairs(GPModule.All) do
            -- Basit isim eşleştirmesi veya tümünü aç
            GPUnlock.Show(gpData) 
        end
    end)
    
    game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Cartola Hub", Text = selectedPass .. " Kilidi Açılıyor...", Duration = 3})
end)

CreateButton(FreePassTab, "Get All Gamepasses (Best) (Mobile) (60% Visual)", function()
    -- Full Bypass Logic (GamepassUnlockController.txt'den)
    spawn(function()
        local success, GP = pcall(function() return require(ReplicatedStorage.Modules.Client.UI.GamepassUnlockController) end)
        if success and GP then
            GP.OwnsGamepass = function() return true end
            if GP.AllPassesFetched ~= nil then GP.AllPassesFetched = true end
        end
        
        -- Görsel kilitleri kaldırma
        local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
        for _, v in pairs(PlayerGui:GetDescendants()) do
            if v:IsA("ImageLabel") and (v.Image:find("lock") or v.Name:lower():find("lock")) then
                v.Visible = false
            end
        end
    end)
    
    -- Remote Sinyalleri
    local remote = ReplicatedStorage:FindFirstChild("RE") or ReplicatedStorage:FindFirstChild("Remotes")
    if remote then
        pcall(function() remote:FindFirstChild("RemoteEvent"):FireServer("UnlockAllPasses", true) end)
    end
    
    game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Full Bypass", Text = "Tüm Gamepassler Aktif Edildi!", Duration = 5})
end)

-- ANA MENÜ ÖZELLİKLERİ
CreateButton(MainTab, "Anti-AFK (Atılma Yok)", function()
    local vu = game:GetService("VirtualUser")
    Players.LocalPlayer.Idled:Connect(function()
        vu:CaptureController()
        vu:ClickButton2(Vector2.new())
    end)
end)

CreateButton(MainTab, "FPS Boost (Kasma Azalt)", function()
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("Part") or v:IsA("MeshPart") then
            v.Material = Enum.Material.SmoothPlastic
        end
    end
    game:GetService("Lighting").GlobalShadows = false
end)

-- EV KONTROL (House)
CreateButton(HouseTab, "Süresiz Ev Bekleme (Cooldown Reset)", function()
    local r = ReplicatedStorage:FindFirstChild("RE") or ReplicatedStorage:FindFirstChild("Remotes")
    if r then pcall(function() r.RemoteEvent:FireServer("UnlockFeature", "VIP_HOUSE_COOLDOWN") end) end
end)

CreateButton(HouseTab, "Evi Anında Yak 🔥", function()
    local r = ReplicatedStorage:FindFirstChild("RE") or ReplicatedStorage:FindFirstChild("Remotes")
    if r then
        pcall(function() r.RemoteEvent:FireServer("HouseSettings", "Fire", true) end)
    end
end)

-- ARAÇLAR
CreateButton(VehicleTab, "Tüm Araç Hızlarını 200 Yap", function()
    for _, v in pairs(Workspace.Vehicles:GetChildren()) do
        local seat = v:FindFirstChild("VehicleSeat", true)
        if seat then seat.MaxSpeed = 200; seat.Torque = 100000 end
    end
end)

CreateToggle(VehicleTab, "Rainbow Araç (RGB)", function(state)
    _G.CarRGB = state
    task.spawn(function()
        while _G.CarRGB do
            local seat = player.Character.Humanoid.SeatPart
            if seat then
                local color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
                pcall(function() ReplicatedStorage.RE["1Player1sCa1r"]:FireServer("PickingCarColor", color) end)
            end
            task.wait(0.1)
        end
    end)
end)

-- AVATAR
CreateButton(AvatarTab, "Karakteri Kopyala (Yakındaki)", function()
    -- Basit en yakın oyuncu bulma
    local target = nil
    local dist = 50
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local d = (player.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
            if d < dist then target = p; dist = d end
        end
    end
    
    if target then
        local desc = target.Character.Humanoid:GetAppliedDescription()
        player.Character.Humanoid:ApplyDescription(desc)
    end
end)

CreateButton(AvatarTab, "Bebek Vücudu (Baby)", function()
    local body = {104157277410075, 86280536086607, 81974054542977, 74524984742501, 122626731952977, 134082579}
    ReplicatedStorage.Remotes.ChangeCharacterBody:InvokeServer(body)
end)

-- TROLL
CreateButton(TrollTab, "Black Hole (Kara Delik)", function()
    -- Optimize edilmiş Black Hole
    local Att = Instance.new("Attachment", Instance.new("Part", Workspace))
    Att.Parent.Anchored = true
    Att.Parent.Transparency = 1
    
    local function Magnet(v)
        if v:IsA("BasePart") and not v.Anchored then
            local Align = Instance.new("AlignPosition", v)
            Align.Attachment0 = Instance.new("Attachment", v)
            Align.Attachment1 = Att
            Align.MaxForce = 999999999
            Align.Responsiveness = 200
        end
    end
    for _, v in pairs(Workspace:GetDescendants()) do Magnet(v) end
    
    task.spawn(function()
        while Att.Parent do
            Att.WorldCFrame = player.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,-15)
            task.wait()
        end
    end)
end)

CreateButton(TrollTab, "FE Görünmezlik", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TROLLLINUXKKK/Fe-invis-vel-/refs/heads/main/Main.txt"))()
end)

-- BAŞLANGIÇ
Pages["Free Gamepass"].Visible = true
MainFrame.Visible = true

print("XenoHub V6 Cartola Edition Yüklendi!")
